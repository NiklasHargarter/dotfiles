# Dotfiles Configuration Repo

This repository uses GNU Stow to manage system configurations. It follows the XDG Base Directory specification, keeping the home directory clean by moving all configurations into ~/.config.

For further instructions visit [the docs](https://niklashargarter.github.io/dotfiles/)
